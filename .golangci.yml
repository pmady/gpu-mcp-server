version: "2"

run:
  timeout: 5m
  build-tags:
    - "!cgo"

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
  exclusions:
    generated: strict
    paths:
      - vendor
